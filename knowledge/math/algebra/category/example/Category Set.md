## Syntax

> [!definition] Category $\text{Set}$
> Category $\text{Set}$ consists of 
> - $\text{Obj}(\text{Set})$ is the class of all sets.
> - For $A, B$ sets, we define
> $$\text{Hom}_\text{Set}(A, B) = B^A$$
