## Problem

> [!definition] The Shortest Vector Problem ($\text{SVP}$)
> Find a shortest nonzero vector in a lattice $L$, i.e., find a nonzero vector $\textbf{v} \in L$ that minimizes the Euclidean norm $||\textbf{v}||$.
