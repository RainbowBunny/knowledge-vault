---
parent: "[[Fleeting MOC]]"
tags:
  - 🪴weedy
date: 2025-11-25T09:28
---
> [!definition] Level of Abstraction
> - **Hardware**: The hardware level, the only physical level, consists of electrical circuits that implement complex combinations of logical operators such as XOR, AND, OR, and NOT gates, known as **digital logic**. Because of its physical nature, hardware cannot be easily manipulated by software. 
> - **Microcode**: The microcode level is also known as **firmware**. Microcode operates only on the exact circuitry for which it was designed. It contains microinstructions that translate from the higher machine-code level to provide a way to interface with the hardware. When performing malware analysis, we usually don’t worry about the microcode because it is often specific to the computer hardware for which it was written.
> - **Machine Code**: The machine code level consists of **opcodes**, hexadecimal digits that tell the processor what you want it to do. Machine code is typically implemented with several microcode instructions so that the underlying hardware can execute the code. Machine code is created when a computer program written in a high-level language is compiled. 
> - **Low-level languages**: A low-level language is a human-readable version of a computer architecture’s instruction set. The most common low-level language is assembly language. Malware analysts operate at the low-level languages level because the machine code is too difficult for a human to comprehend. We use a disassembler to generate low-level language text, which consists of simple mnemonics such as mov and jmp. Many different dialects of assembly language exist, and we’ll explore each in turn.
> - **High-level languages**: Most computer programmers operate at the level of high-level languages. High-level languages provide strong abstraction from the machine level and make it easy to use programming logic and flow-control mechanisms. High-level languages include C, C++, and others. These languages are typically turned into machine code by a compiler through a process known as **compilation**. 
> - **Interpreted languages**: Interpreted languages are at the top level. Many programmers use interpreted languages such as C#, Perl, .NET, and Java. The code at this level is not compiled into machine code; instead, it is translated into bytecode. **Bytecode** is an intermediate representation that is specific to the programming language. Bytecode executes within an **interpreter**, which is a program that translates bytecode into executable machine code on the fly at runtime. An interpreter provides an automatic level of abstraction when compared to traditional compiled code, because it can handle errors and memory management on its own, independent of the OS.

# x86

## Data types

| Type    | Bits | Byte          | Aliases                                          |
| ------- | ---- | ------------- | ------------------------------------------------ |
| n/a     | 4    | $\frac{1}{2}$ | nibble, semioctet (rarely mentioned)             |
| `BYTE`  | 8    | 1             | byte, octet, char                                |
| `WORD`  | 16   | 2             | word, short                                      |
| `DWORD` | 32   | 4             | long, doubleword, longword, int, int32           |
| `QWORD` | 64   | 8             | longword, long long, quadword, int64             |
| n/a     | 128  | 16            | octaword, double quadword (for data heavy maths) |

## x86 Instruction Data Structure:

### Data structure of a single instruction

| 0-4 bytes | 1-3 bytes | 0-1 byte      | 0-1 byte                 | 0, 1, 2, 4 bytes | 0, 1, 2, 4, 8 bytes |
| --------- | --------- | ------------- | ------------------------ | ---------------- | ------------------- |
| `Prefix`  | `Opcode`  | `Mod-Reg R/M` | `Scale-Index-Base (SIB)` | `Displacement`   | `Immediate`         |

### Prefix

| Prefix                                                                            | Bytes                                                                                                                        | Effect                                              |
| --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| [`Legacy`](https://wiki.osdev.org/X86-64_Instruction_Encoding#Legacy_Prefixes)    | G1: `0xF0`, `0xF2`, `0xF3`<br>G2: `0x2E`, `0x36`, `0x3E`, `0x26`, `0x64`, `0x65`, `0x2E`, `0x3E`<br>G3: `0x66`<br>G4: `0x67` | Mandatory for some older instructions.              |
| [`REX`](https://wiki.osdev.org/X86-64_Instruction_Encoding#REX_prefix)            | `0b0100WRXB`                                                                                                                 | Enables 64-bit operand size and extended registers. |
| [`VEX/XOP`](https://wiki.osdev.org/X86-64_Instruction_Encoding#VEX.2FXOP_opcodes) | _2-3 bytes,                                                                                                                  | Vector math extensions (3 operands)                 |
#### Segment Register Prefix Byte

| Mnemonic | Byte   | Name          | Legacy x86 Purpose                     | x64 Purpose                           |
| -------- | ------ | ------------- | -------------------------------------- | ------------------------------------- |
| `SS`     | `0x2e` | Stack Segment | Pointer to process stack.              | Pointer to `0x0`; unused.             |
| `CS`     | `0x36` | Code Segment  | Pointer to process code.               | Pointer to `0x0`; unused.             |
| `DS`     | `0x3e` | Data Segment  | Pointer to process data. (ie. strings) | Pointer to `0x0`; unused.             |
| `ES`     | `0x26` | Extra Segment | Pointer to extra data. (User defined)  | Pointer to `0x0`; unused.             |
| `FS`     | `0x64` | F Segment     | Pointer to extra data. (User defined)  | Pointer to thread-local process data. |
| `GS`     | `0x65` | G Segment     | Pointer to extra data. (User defined)  | Pointer to thread-local process data. |
#### REX Prefix Byte Data Structure (8 bits)

| Field    | Bit Length | Effect                                                                                                    |
| -------- | ---------- | --------------------------------------------------------------------------------------------------------- |
| `0b0100` | 4          | Constant; recognizable magic prefix.                                                                      |
| `W`      | 1          | `1`: 64-bit operand size (ie. `RAX`)  <br>`0`: Default operand size (usually 32-bit, but per-instruction) |
| `R`      | 1          | `1`: Prepend `MODRM.(R)eg` by `1`-bit to map registers `R8`-`R15`.                                        |
| `X`      | 1          | `1`: Prepend `SIB.inde(X)` by `1`-bit to map registers `R8`-`R15`.                                        |
| `B`      | 1          | `1`: Prepend `MODRM.rm` and `SIB.(B)ase` by `1`-bit to map registers `R8`-`R15`.                          |
## The Operation Code (Opcode)

## The General Purpose Registers

Example:

| Register | Value                 |
| -------- | --------------------- |
| `RAX`    | `0000 0001 0002 0003` |
| `EAX`    | `          0002 0003` |
| `AX`     | `               0003` |
| `AH`     | `               00`   |
| `AL`     | `                 03` |

| 8-byte register | Bytes 0-3 | Bytes 0-1 | Byte 0 | Name                  | Commonly used as                                                                                           | Reference |
| :-------------: | :-------: | :-------: | :----: | --------------------- | ---------------------------------------------------------------------------------------------------------- | --------- |
|      %rax       |   %eax    |    %ax    |  %al   | **Accumulator**       | Return value, especially the sum of arithmetic operations.                                                 | `0b000`   |
|      %rcx       |   %ecx    |    %cx    |  %cl   | **Counter**<br>       | Used by loops ie. the `i` in `for(int i=0; i<9; i++)`                                                      | `0b001`   |
|      %rdx       |   %edx    |    %dx    |  %dl   | **Data**              | Extended space for accumulator.  (ie. `32`-bit mode will combine `EAX+EDX` to work on `64`-bit values)     | `0b010`   |
|      %rbx       |   %ebx    |    %bx    |  %bl   | **Base index**        | Starting point of an array or list structure.                                                              | `0b011`   |
|      %rsp       |   %esp    |    %sp    |  %spl  | **Stack Pointer**     | Pointer to address of last bytes `PUSH`ed to memory.                                                       | `0b100`   |
|      %rbp       |   %ebp    |    %bp    |  %bpl  | **Base Pointer**      | Pointer to address of current stack frame.  <br>(where function parameters end, and local variables begin) | `0b101`   |
|      %rsi       |   %esi    |    %si    |  %sil  | **Source Index**      | Starting point of unbounded stream data, especially a string.                                              | `0b110`   |
|      %rdi       |   %edi    |    %di    |  %dil  | **Destination Index** | Ending point of unbounded data, especially in slicing operations.                                          | `0b111`   |
|       %r8       |   %r8d    |   %r8w    |  %r8b  |                       |                                                                                                            |           |
|       %r9       |   %r9d    |   %r9w    |  %r9b  |                       |                                                                                                            |           |
|      %r10       |   %r10d   |   %r10w   | %r10b  |                       |                                                                                                            |           |
|      %r11       |   %r11d   |   %r11w   | %r11b  |                       |                                                                                                            |           |
|      %r12       |   %r12d   |   %r12w   | %r12b  |                       |                                                                                                            |           |
|      %r13       |   %r13d   |   %r13w   | %r13b  |                       |                                                                                                            |           |
|      %r14       |   %r14d   |   %r14w   | %r14b  |                       |                                                                                                            |           |
|      %r15       |   %r15d   |   %r15w   | %r15b  |                       |                                                                                                            |           |
The instruction pointer **eip** and **flags** register are extended to 64 bits (**rip** and **rflags**, respectively).

The x64 processor also provides several sets of floating-point registers:
- Eight 80-bit x87 registers.
- Eight 64-bit MMX registers. (These registers overlap with the x87 registers.)
- The original set of eight 128-bit SSE registers is increased to 16.

## The `Memory` Address Operand
### Operand types

The basic types of operand specifiers are below. In the following table,
- `Imm` refers to a constant value, e.g. `0x8048d8e` or `48`
- $E_x$ refers to a register, e.g. `%rax`
- $R[E_x]$ refers to the value stored in register $E_x$, and
- $M[x]$ refers to the value stored at memory address $x$.

| Type      | From                 | Operand Value                            | Name           |
| --------- | -------------------- | ---------------------------------------- | -------------- |
| Immediate | `$Imm`               | `Imm`                                    | Immediate      |
| Register  | $E_a$                | $R[E_a]$                                 | Register       |
| Memory    | `Imm`                | `M[Imm]`                                 | Absolute       |
| Memory    | $(E_a)$              | $M[R[E_b]]$                              | Absolute       |
| Memory    | `Imm`$(E_b, E_i, s)$ | $M[$`Imm`$+ R[E_b] + (R[E_i] \times s)]$ | Scaled indexed |
`Register` Operand (1 byte `MOD-REG R/M)

| Data Structure           | Size                   | Required |
| ------------------------ | ---------------------- | -------- |
| `Mod-Reg R/M`            | `8`-bits               | Required |
| `Scale-Index-Base (SIB)` | `8`-bits               | Required |
| `Displacement`           | `0`/`8`/`16`/`32`-bits | Optional |

| 2-bits (0-4)  <br>`MODRM.mod` | 3-bits (0-7)  <br>`MODRM.reg` (reg/opcode) | 3-bits (0-7)  <br>`MODRM.rm` (register/memory) |
| ----------------------------- | ------------------------------------------ | ---------------------------------------------- |
| `0b00`  <br>                  | Memory                                     | Memory, no displacement                        |
| `0b01`                        | Memory                                     | Memory, `8-bit` displacement                   |
| `0b10`                        | Memory                                     | Memory, `32-bit` displacement                  |
| `0b11`                        | `register`                                 | `register`                                     |
The structure of `SIB` is, briefly:

| `2`-bits | `3`-bits | `3`-bits |
| -------- | -------- | -------- |
| `Scale`  | `Index`  | `Base`   |

When calculating the address, the formula is, generally:
**Real Address** = `Segment` + `SIB.base` + (`SIB.index` × `SIB.scale`) + `Displacement`
Where:

| Variable       | Meaning                                                                                                                                                                                          |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Segment`      | Augend to the following variables. Remember most segments are mapped to `0x00` in `Long` mode.                                                                                                   |
| `SIB.base`     | Refers to a register, whose value holds the augend to the product of `SIB.scale` and `SIB.index`.                                                                                                |
| `SIB.scale`    | Multiplicand of `SIB.index`: `0b00`=×1, `0b01`=×2, `0b10`=×4, `0b11`=×8                                                                                                                          |
| `SIB.index`    | Refers to a register, whose value holds the multiplier of `SIB.scale`.                                                                                                                           |
| `Displacement` | Literal value, holds an actual relative address; an addend to all previous variables.  <br>If no `SIB` byte is present in `32`-bit mode, address is relative to `RIP`/`EIP` instruction pointer. |


## Calling convention

### Microsoft x64 ABI (Windows)

Unlike the x86 architecture, the C/C++ compiler supports only one calling convention on x64. This calling convention takes advantage of the increased number of registers available on x64:
- The first four integer or pointer parameters are passed in the **rcx**, **rdx**, **r8**, and **r9** registers.
- The first four floating-point parameters are passed in the first four SSE registers, **xmm0**-**xmm3**.
- The caller reserves space on the stack for arguments passed in registers. The called function can use this space to spill the contents of registers to the stack.
- You pass any other arguments on the stack.
- An integer or pointer return value is returned in the **rax** register, while a floating-point return value is returned in **xmm0**.
- **rax**, **rcx**, **rdx**, **r8**-**r11** are volatile.
- **rbx**, **rbp**, **rdi**, **rsi**, **r12**-**r15** are nonvolatile.

### System V AMD64 ABI (Linux, macOS, BSD)

1. User-level applications use as integer registers for passing the sequence `%rdi,%rsi,%rdx,%rcx,%r8,%r9`. The kernel interface uses `%rdi,%rsi,%rdx,%r10,%r8,%r9`.
2. A system-call is done via the `syscall` instruction. The kernel destroys register `%rcx` and `%r11`. The reason of this is the hardware instruction `syscall`: loads `rip` from the model-specific register (`msr`), saves old `rip` by `rcx` and `rflags` by `r11`.
3. The number of the `syscall` has to be passed in register `rax` (`write = 1, open = 2, close = 3, exit = 60, read = 0, ...`)
4. System-calls are limited to six arguments, no argument is passed directly on the stack because the kernel must not trust user stack memory.
5. Returning from the `syscall`, register `rax` contains the result of the system-call. A value in the range between -4095 and -1 indicates an error, it is -errno (*negated errno*).
6. Only values of class INTEGER or class MEMORY are passed to the kernel.

Result is stored at `%rax`.

# x64 Instructions


## Data Movement

| Instruction | Operands | Description                       | Effect                                      |
| ----------- | -------- | --------------------------------- | ------------------------------------------- |
| `mov`       | `D, S`   | Move source to destination        | `D = S`                                     |
| `push`      | `S`      | Push source onto stack            | Store `S` and decrease `rsp` by 8.          |
| `pop`       | `D`      | Pop top of stack into destination | Loads value to `D` and increase `rsp` by 8. |

## Arithmetic Operations

### Unary Operations

| Instruction | Operands | Description         | Effect      |
| ----------- | -------- | ------------------- | ----------- |
| `inc`       | `D`      | Increment by 1      | `D = D + 1` |
| `dec`       | `D`      | Decrement by 1      | `D = D - 1` |
| `neg`       | `D`      | Arithmetic negation | `D = -D`    |
| `not`       | `D`      | Bitwise complement  | `D = ~D`    |

### Binary Operations

| Instruction | Operands     | Description                                       | Effect        |
| ----------- | ------------ | ------------------------------------------------- | ------------- |
| `add`       | `D, S`       | Add source to destination                         | `D = D + S`   |
| `sub`       | `D, S`       | Subtract source from destination                  | `D = D - S`   |
| `lea`       | `D, address` | Load effective address of source into destination | `D = address` |
| `imul`      | `D, S`       | Multiply destination by source                    | `D = D * S`   |
| `xor`       | `D, S`       | Bitwise XOR destination by source                 | `D = `        |
| `or`        | `D, S`       | Bitwise OR destination by source                  | `D = D \| S`  |
| `and`       | `D, S`       | Bitwise AND destination by source                 | `D = D & S`   |



### Shift Operations

| Instruction | Operands | Description                                    | Effect                            |
| ----------- | -------- | ---------------------------------------------- | --------------------------------- |
| `sal/shl`   | `D, k`   | Left shift destination by `k` bits             | `D = D << k`                      |
| `sar`       | `D, k`   | Arithmetic right shift destination by `k` bits | `D = D >> k` (keeping the MSB)    |
| `shr`       | `D, k`   | Logical right shift destination by `k` bits    | `D = D >> k` (also shift the MSB) |

### Special Arithmetic Operations

| Instruction | Operands | Description                                                                           | Effect                                           |
| ----------- | -------- | ------------------------------------------------------------------------------------- | ------------------------------------------------ |
| `imulq`     | `S`      | Signed full multiply of %rax by S<br>Result stored in %rdx:%rax                       | `%rdx * 2^64 + %rax = %rax * S`                  |
| `mulq`      | `S`      | Unsigned full multiply of %rax by S<br>Result stored in %rdx:%rax                     | `%rdx * 2^64 + %rax = %rax * S`                  |
| `idivq`     | `S`      | Signed divide %rdx:%rax by S<br>Quotient stored in %rax <br>Remainder stored in %rdx  | `%rax = %rdx:%rax / S`<br>`%rdx = %rdx:%rax % S` |
| `divq`      | `S`      | Unsigned divide %rdx:%rax by S<br>Quotient stored in %rax<br>Remainder stored in %rdx | `%rax = %rdx:%rax / S`<br>`%rdx = %rdx:%rax % S` |

## Comparison and Test Instructions

| Instruction | Operands   | Description                                   |
| ----------- | ---------- | --------------------------------------------- |
| `cmp`       | $S_2, S_1$ | Set condition codes according to $S_1 - S_2$  |
| `test`      | $S_2, S_1$ | Set condition codes according to $S_1 \& S_2$ |

## Accessing Condition Codes


## Procedure Call Instruction

| Instruction | Operands      | Description                                              | Effect                                    |
| ----------- | ------------- | -------------------------------------------------------- | ----------------------------------------- |
| `call`      | Label/Operand | Push return address and jump to label/specified location | `push return_address` then `jmp function` |
| `leave`     |               | Set `%rsp` to `%rbp`, then pop top of stack into `%rbp`  | `rsp = rbp` then `pop rbp`                |
| `ret`       |               | Pop return address from stack and jump there             | `pop rip`                                 |

# x64 Flags

## Status Flags (affected by arithmetic/logic)

| Flag | Bit | Name          | Meaning                             |
| ---- | --- | ------------- | ----------------------------------- |
| `CF` | 0   | Carry Flag    | Carry/borrow in unsigned arithmetic |
| `PF` | 2   | Parity Flag   | Even parity of low 8 bits           |
| `AF` | 4   | Adjust Flag   | BCD/legacy (rarely used)            |
| `ZF` | 6   | Zero Flag     | Result equal to 0                   |
| `SF` | 7   | Sign Flag     | MSB of result                       |
| `OF` | 11  | Overflow Flag | Two's-complement signed overflow    |

## Control Flags

| Flag | Bit | Name           | Meaning                                     |
| ---- | --- | -------------- | ------------------------------------------- |
| `TF` | 8   | Trap Flag      | Single-step debugging                       |
| `IF` | 9   | Interrupt Flag | Enables interrupts                          |
| `DF` | 10  | Direction Flag | Affects string ops (`inc/dec` of `rsi/rdi`) |

## System Flags (rarely touched manually) 

| Flag      | Bit   | Name                           |
| --------- | ----- | ------------------------------ |
| `IOPL`    | 12-13 | I/O priviledge                 |
| `NT`      | 14    | Nested Task                    |
| `RF`      | 16    | Resume Flag                    |
| `VM`      | 17    | Virtual-8086 Mode              |
| `AC`      | 18    | Alignment Check                |
| `VIF/VIP` | 19-20 | Virtual IF / Interrupt pending |
| `ID`      | 21    | CPUID enable                   |


# Syscall table

[Link](https://github.com/torvalds/linux/blob/master/arch/x86/entry/syscalls/syscall_64.tbl)

# Opcode Table

[https://en.wikipedia.org/wiki/X86_instruction_listings](https://en.wikipedia.org/wiki/X86_instruction_listings)
[http://ref.x86asm.net/coder64.html](http://ref.x86asm.net/coder64.html)
- `moffs`: Memory offset.
- `imm`: Immediate.
- `Sreg`: Segment registers.
- `r/m`: Register or memory
`40 - 4F`: REX prefix 
Note: 
- Jump operation offset is relative to the next operation.
- `TEST`:

| Opcode | Instructions | Notes                                                               | Effects                           |
| ------ | ------------ | ------------------------------------------------------------------- | --------------------------------- |
| `E8`   | `CALL`       | `CALL` operation with signed 32-bit relative offset: `E8` + `rel32` | `push RIP`<br>`RIP = RIP + rel32` |
| `98`   | `CBW`        | Convert `Byte` to `Word`                                            | `AX = AL`, signed extended        |
| `98`   | `CWQE/CDQE`  | Convert `Doubleword` to `Quadword` extended                         | `RAX = EAX`, signed extended      |
| `3D`   | `CMP`        | First operand: `RAX`<br>Second operand: `imm16/32`                  | Sub of two operands               |
| `70`   | `JO`         | Jump if overflow <br>Operand: 8 bit offset `rel8`                   | Flag `OF = 1`                     |
| `71`   | `JNO`        | Jump if no overflow <br>Operand: 8 bit offset `rel8`                | Flag `OF = 0`                     |
| `72`   | `JB/JC`      | Jump if below / carry <br>Operand: 8 bit offset `rel8`              | Flag `CF = 1`                     |
| `73`   | `JAE/JNC`    | Jump if above or equal <br>Operand: 8 bit offset `rel8`             | Flag `CF = 0`                     |
| `74`   | `JE/JZ`      | Jump if equal / zero <br>Operand: 8 bit offset `rel8`               | Flag `ZF = 1`                     |
| `75`   | `JNE/JNZ`    | Jump if not equal <br>Operand: 8 bit offset `rel8`                  | Flag `ZF = 0`                     |
| `76`   | `JBE`        | Jump if below or equal <br>Operand: 8 bit offset `rel8`             | Flag `CF = 1` or `ZF = 1`         |
| `77`   | `JA`         | Jump if above <br>Operand: 8 bit offset `rel8`                      | Flag `CF = 0` and `ZF = 0`        |
| `78`   | `JS`         | Jump if negative <br>Operand: 8 bit offset `rel8`                   | Flag `SF = 1`                     |
| `79`   | `JNS`        | Jump if non-negative <br>Operand: 8 bit offset `rel8`               | Flag `SF = 0`                     |
| `7A`   | `JP/JPE`     | Jump if parity even <br>Operand: 8 bit offset `rel8`                | Flag `PF = 1`                     |
| `7B`   | `JNP/JPO`    | Jump if parity odd <br>Operand: 8 bit offset `rel8`                 | Flag `PF = 0`                     |
| `7C`   | `JL`         | Jump if less (signed) <br>Operand: 8 bit offset `rel8`              | Flag `SF != OF`                   |
| `7D`   | `JGE`        | Jump if `>=` (signed) <br>Operand: 8 bit offset `rel8`              | Flag `SF = OF`                    |
| `7E`   | `JLE`        | Jump if `<=` (signed) <br>Operand: 8 bit offset `rel8`              | Flag `ZF = 1` or `SF != OF`       |
| `7F`   | `JG`         | Jump if `>` (signed) <br>Operand: 8 bit offset `rel8`               | Flag `ZF = 0` and `SF = OF`       |
| `8B`   | `MOV`        | First operand: `r16/32/64`<br>Second Operand: `r/m16/32/64`         |                                   |
| `8C`   | `MOV`        | First operand: `r/m16`<br>Second operand: `Sreg`                    |                                   |
| `8E`   | `MOV`        | First operand: `Sreg`<br>Second operand: `r/m16`                    |                                   |
| `A0`   | `MOV`        | First operand: `AL`<br>Second operand: `moffs8`                     |                                   |
| `A1`   | `MOV`        | First operand: `AX\|EAX\|RAX`<br>Second operand: `moffs`            |                                   |
| `A2`   | `MOV`        | First operand: `moffs8`<br>Second operand: `AL`                     |                                   |
| `A3`   | `MOV`        | First operand: `moffs`<br>Second operand: `AX\|EAX\|RAX`            |                                   |
| `B0+r` | `MOV`        | First operand: `r8`<br>Second operand: `imm8`                       |                                   |
| `B8+r` | `MOV`        | First operand: `r16\|32\|64`<br>Second operand: `imm16\|32\|64`     |                                   |
| `C6`   | `MOV`        | First operand: `r/m8`<br>Second operand: `imm8`                     |                                   |
| `C7`   | `MOV`        | First operand: `r/m16/32/64`<br>Second operand: `imm16/32`          | `MODRM.reg = 0`                   |
| `84`   | `TEST`       | First operand: `r/m8`<br>Second operand: `r8`                       |                                   |
| `85`   | `TEST`       | First operand: `r/m16\|32\|64`<br>Second operand: `r16\|32\|64`     |                                   |
| `A8`   | `TEST`       | First operand: `AL`<br>Second operand: `imm8`                       |                                   |
| `A9`   | `TEST`       | First operand: `AX\|EAX\|RAX`<br>Second operand: `imm16\|32`        |                                   |
| `F6`   | `TEST`       | First operand: `r/m8`<br>Second operand: `imm8`                     |                                   |
| `F7`   | `TEST`       | First operand: `r/m16\|32\|64`<br>Second operand: `imm`             |                                   |
|        |              |                                                                     |                                   |
|        |              |                                                                     |                                   |

Prefix `0F`

| Opcode  | Instruction | Notes | Effects |
| ------- | ----------- | ----- | ------- |
| `0F 05` | `SYSCALL`   |       |         |







